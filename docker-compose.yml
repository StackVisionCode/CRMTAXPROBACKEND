services:
  auth-service:
    build:
      context: .
      dockerfile: AuthService/Dockerfile
    ports:
      - "5092:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development   
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal,1433;Database=AuthDB;User=sa;Password=Brittany040238.;TrustServerCertificate=True;Encrypt=false;MultipleActiveResultSets=true
    networks:
      - crmtaxpro-network

  customer-service:
    build:
      context: .
      dockerfile: CustomerService/Dockerfile
    ports:
      - "5094:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal,1433;Database=CustomerDB;User=sa;Password=Brittany040238.;TrustServerCertificate=True;Encrypt=false;MultipleActiveResultSets=true
    networks:
      - crmtaxpro-network

networks:
  crmtaxpro-network:
    driver: bridge
