# =============================================================
#  CI-CD ▸ Microservicios .NET 9 → Docker → Windows Server 2019
# =============================================================

name: CI-CD Docker (.NET 9)

on:
  push:
    branches: [ main ]          # Cámbialo si tu rama principal es otra
  workflow_dispatch:

# ---------- Variables globales ----------
env:
  REGISTRY: docker.io
  REGISTRY_PREFIX: jturbi/crmtaxpro   # docker.io/jturbi/crmtaxpro-<svc>
  TAG: ${{ github.sha }}              # Versión única por commit

# =============================================================
# 1) Build & Push  (runner Ubuntu hospedado por GitHub)
# =============================================================
jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: jturbi
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & push images (.NET 9)
        shell: bash
        run: |
          declare -A DOCKERS=(
            [apigateway]=ApiGateway/Dockerfile
            [auth-service]=AuthService/Dockerfile
            [customer-service]=CustomerService/Dockerfile
            [docu-service]=SignDocuTax/Dockerfile
            [email-service]=EmailServices/Dockerfile
          )
          for svc in "${!DOCKERS[@]}"; do
            dockerfile="${DOCKERS[$svc]}"
            image="$REGISTRY/$REGISTRY_PREFIX-$svc"
            echo "🔨 Building $image…"
            docker build -f "$dockerfile" -t "$image:$TAG" -t "$image:latest" .
            docker push "$image" --all-tags
          done

# =============================================================
# 2) Deploy  (runner self-hosted Windows Server 2019)
# =============================================================
  deploy:
    needs: build-and-push
    runs-on: self-hosted
    timeout-minutes: 30

    defaults:
      run:
        shell: powershell   # Windows PowerShell (v1)

    steps:
      - uses: actions/checkout@v4

      # 1️⃣  Login a Docker Hub
      - name: Docker login
        run: docker login $Env:REGISTRY -u jturbi -p "${{ secrets.DOCKERHUB_TOKEN }}"

      # 2️⃣  Crear archivo .env con versión y prefijo
      - name: Crear .env para Compose
        run: |
          "REGISTRY_PREFIX=$Env:REGISTRY_PREFIX" | Out-File .env -Encoding utf8
          "TAG=$Env:TAG"                         | Add-Content .env -Encoding utf8
          Get-Content .env

      # 3️⃣  Descargar imágenes publicadas
      - name: Pull images
        run: docker-compose --env-file .env -f docker-compose.prod.yml pull

      # 4️⃣  Levantar / actualizar contenedores
      - name: Up / Update containers
        run: |
          docker-compose --env-file .env -f docker-compose.prod.yml up -d --remove-orphans
          docker image prune -f        # Limpieza opcional



